	mc68000
	opt o-

; =========================================================
; LSL.b Dx, Dy
; =========================================================

	LSL.b D7,D7
	LSL.b D6,D6
	LSL.b D5,D5
	LSL.b D4,D4
	LSL.b D3,D3
	LSL.b D2,D2
	LSL.b D1,D1
	LSL.b D0,D0

; =========================================================
; LSL.w Dx, Dy
; =========================================================

	LSL.w D7,D7
	LSL.w D6,D6
	LSL.w D5,D5
	LSL.w D4,D4
	LSL.w D3,D3
	LSL.w D2,D2
	LSL.w D1,D1
	LSL.w D0,D0

; =========================================================
; LSL.l Dx, Dy
; =========================================================

	LSL.l D7,D7
	LSL.l D6,D6
	LSL.l D5,D5
	LSL.l D4,D4
	LSL.l D3,D3
	LSL.l D2,D2
	LSL.l D1,D1
	LSL.l D0,D0

; =========================================================
; LSL.b #<data>, Dy
; =========================================================

	LSL.b #1,D7
	LSL.b #2,D6
	LSL.b #3,D5
	LSL.b #4,D4
	LSL.b #5,D3
	LSL.b #6,D2
	LSL.b #7,D1
	LSL.b #8,D0

; =========================================================
; LSL.w #<data>, Dy
; =========================================================

	LSL.w #1,D7
	LSL.w #2,D6
	LSL.w #3,D5
	LSL.w #4,D4
	LSL.w #5,D3
	LSL.w #6,D2
	LSL.w #7,D1
	LSL.w #8,D0

; =========================================================
; LSL.l #<data>, Dy
; =========================================================

	LSL.l #1,D7
	LSL.l #2,D6
	LSL.l #3,D5
	LSL.l #4,D4
	LSL.l #5,D3
	LSL.l #6,D2
	LSL.l #7,D1
	LSL.l #8,D0